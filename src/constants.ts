/** Fixed refusal message for insufficient-evidence results (FR-AB-3). */
export const INSUFFICIENT_EVIDENCE_MESSAGE =
  "I don't have enough information in the provided documents to answer that.";

export const INSUFFICIENT_EVIDENCE_STATUS = "insufficient_evidence" as const;
export const ANSWERED_STATUS = "answered" as const;
